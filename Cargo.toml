[package]
name = "ctrlflow"
version = "0.1.0"
authors = ["Fenhl <fenhl@fenhl.net>"]
edition = "2018"

[dependencies]
async-stream = "0.2"
futures = "0.3"
itertools = "0.9"
petgraph = "0.5"

[dependencies.async-std]
version = "1"
features = ["unstable"]
optional = true

[dependencies.tokio]
version = "0.2"
optional = true
features = ["rt-core", "sync"]
